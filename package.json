{
  "name": "fitquest",
  "version": "0.1.0",
  "private": true,
  "description": "Personal fitness RPG with D&D-style stat sheet, Habitica-inspired gamification",
  "workspaces": [
    "api",
    "web"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c cyan,magenta \"npm:dev:api\" \"npm:dev:web\"",
    "dev:api": "npm --workspace api run dev",
    "dev:web": "npm --workspace web run dev",
    "build": "npm --workspace api run build && npm --workspace web run build",
    "db:migrate": "npm --workspace api run db:migrate",
    "db:seed": "npm --workspace api run db:seed",
    "typecheck": "npm --workspace api run typecheck && npm --workspace web run typecheck",
    "test": "npm --workspace api run test",
    "test:watch": "npm --workspace api run test:watch"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
