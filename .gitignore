# dependencies
node_modules/
.pnp
.pnp.js

# testing
coverage/
*.lcov

# production
dist/
build/
.next/
out/

# misc
.DS_Store
*.pem
.vscode/
.idea/

# env
.env
.env.local
.env.*.local

# logs
logs/
*.log
npm-debug.log*

# prisma
api/prisma/migrations/dev.db*

# misc
*.tsbuildinfo

# Python
__pycache__/
*.pyc

# Capacitor / Android wrapper artifacts (the .apk itself is the
# release artifact, not source — the user builds the APK with
# `cd web && npx cap sync android && cd android && ./gradlew assembleDebug`)
web/android/
web/dist-mobile/
# Stray root-level mobile build dir (mirrored above; the user's
# local release script outputs to repo root in some setups).
/dist-mobile/
